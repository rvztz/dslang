program factorial;
func int factorial(int x) {
    if (x == 0) {
        return 1;
    }
    i := x;
    fact := 1;
    while ((x / i) != x) do {
        fact = fact * i;
        i = i - 1;
    }
    return fact;
}
fact := factorial$(5);
write(fact);